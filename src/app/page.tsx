import { MapWrapper } from "@/components/mapbox/mapbox-wrapper";

export default function HomePage() {
  return (
    <div className="flex flex-1 items-center justify-center">
      <MapWrapper />
    </div>
  );
}
